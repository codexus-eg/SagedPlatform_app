import 'package:flutter/material.dart';

class AppTextStyles {
  // tilte behind back btn
  static const headStyle = TextStyle(
    fontSize: 48,
    fontWeight: FontWeight.w900,
  );

// wallet, balance
  static const title1Style = TextStyle(
    fontSize: 24,
    fontWeight: FontWeight.w900,
  );

  // lecture title, balance
  static const title2Style = TextStyle(
    fontSize: 20,
    fontWeight: FontWeight.w900,
  );

  // group name, request
  static const body1Style = TextStyle(
    fontSize: 20,
  );

  // wallet, clickable text, desc, std name
  static const body2Style = TextStyle(
    fontSize: 16,
  );
}
